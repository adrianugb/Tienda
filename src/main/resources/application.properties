spring.application.name=Tienda
#Se define el puerto por donde escucha el servidor web
server.port=80

#Se define el formato del log
logging.pattern.dateformat=hh:mm

#Se elimina el banner que dice Spring
spring.main.banner-mode=off

#Se evita el uso de cache por el servidor en modo desarrollo(false)
#En modo producción SI se requiere(true)
spring.thymeleaf.cache=false

